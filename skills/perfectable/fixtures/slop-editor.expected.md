# slop-editor

Detector exits 2. Expect at least: `glass-on-content`, `marketing-radius-on-row`, `ios-body-in-chrome`, `centered-hero-in-shell`. `overused-dev-font` and `ai-ide-palette` may also fire.

A layout pass names these, whether or not the scanner saw them:

- Composition is missing. The squint test sees a centered hero, not a buffer.
- One visual idea (a card in the middle) instead of editor-first.
- Air is around the card. The sovereign surface should be the buffer, with the margin inside the measure, not a void around a pill.
- The row wears a marketing radius. Chrome type uses the phone body size.
- Blur sits on the editor. That is the content layer.
