# slop-dashboard

Detector reports `metric-card-wall`. `equal-pane-grid` also matches `repeat(4, 1fr)`.

A layout pass names:

- This is not `briefing`, `exception-board`, or `ledger`. Four equal metric cards have no sovereign reading.
- One interval, 16px, for the grid and the cards.
- Air is the gutter between tiles. In a briefing it belongs to the primary figure and the list that explains it.
