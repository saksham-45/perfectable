# settings

Detector exits 0.

A layout pass accepts it:

- Composition is settings. Search is first. One group, so no category sidebar.
- Labels share a rail. Two intervals are visible: the field gap and the group gap.
- It is a form in a window, not a modal and not a row of metric cards.
