# editor-first

Detector exits 0.

A layout pass accepts the composition:

- Sovereign surface is the buffer. Sidebar and status are chrome.
- The row is dense and slightly rounded. Prose is capped. Code would use the pane.
- Air is the padding inside the editor, not a growing titlebar.
- Do not "improve" this into a card, a hero, or three equal columns.
