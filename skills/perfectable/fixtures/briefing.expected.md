# briefing

Detector exits 0.

A layout pass accepts it as `briefing`:

- The figure is sovereign. The table explains it. They are not equal cards.
- Rows are dense. The figure has its own inset. Two intervals are visible.
- Do not "improve" this into a wall of metric cards.
